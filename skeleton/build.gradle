// This is for the generation of the WAR file
war {
    version ""
    archiveName "ROOT.war"
}

assets {
    minifyOptions = [
        languageMode: 'ES6',
        targetLanguage: 'ES5', //Can go from ES6 to ES5 for those bleeding edgers
        optimizationLevel: 'SIMPLE'
    ]
}